# 🎯 Arquitetura do Serviço

## Visão Geral

```
┌─────────────────────────────────────────────────────────────────┐
│                         Sistemas Externos                        │
│  ┌──────────────────┐  ┌──────────────┐  ┌─────────────────┐   │
│  │  Frontend React  │  │   App Mobile │  │  Backend Flask  │   │
│  │   localhost:3000 │  │ localhost:3001 │  │ localhost:8000  │   │
│  └────────┬─────────┘  └──────┬───────┘  └────────┬────────┘   │
└───────────┼──────────────────┼────────────────────┼─────────────┘
            │                  │                    │
            └──────────────────┼────────────────────┘
                               │
                    ┌──────────▼──────────┐
                    │   API REST FastAPI  │
                    │   :8003             │
                    └──────────┬──────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
   ┌────▼────┐          ┌──────▼──────┐       ┌──────▼──────┐
   │  Upload │          │  Processing │       │  Queries    │
   │ Routes  │          │  Routes     │       │  Routes     │
   └────┬────┘          └──────┬──────┘       └──────┬──────┘
        │                      │                      │
        └──────────┬───────────┴──────────┬───────────┘
                   │                      │
        ┌──────────▼──────────┐  ┌───────▼────────────┐
        │  Services Layer     │  │  Integration Layer │
        ├─────────────────────┤  ├────────────────────┤
        │ AudioProcessing     │  │ BackendIntegration │
        │ Service             │  │ AppConectaIntegration
        └──────────┬──────────┘  └────────────────────┘
                   │
      ┌────────────┼────────────┐
      │            │            │
 ┌────▼────┐  ┌────▼────┐ ┌────▼─────────┐
 │ Trans-  │  │ Summar- │ │  Database    │
 │ cription│  │ ization │ │  Operations  │
 │ Agent   │  │ Agent   │ │              │
 └────┬────┘  └────┬────┘ └────┬─────────┘
      │            │            │
      └────────────┼────────────┘
                   │
        ┌──────────▼──────────┐
        │   PostgreSQL 15     │
        │   Database          │
        ├─────────────────────┤
        │ AudioRecords        │
        │ Transcriptions      │
        │ Summarizations      │
        └─────────────────────┘
```

## Fluxo de Processamento

```
1. UPLOAD INICIAL
   User/App ──────┐
                  ▼
           POST /api/v1/audio/upload
                  │
                  ▼
        ┌────────────────────┐
        │ Validação de Input │
        │ - Agent ID         │
        │ - Patient ID       │
        │ - Arquivo          │
        └────────┬───────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ Criar AudioRecord  │
        │ Status: uploaded   │
        └────────┬───────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ Salvar Arquivo     │
        │ em /tmp            │
        └────────┬───────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ Retornar ID        │
        │ (para consultas)   │
        └────────┬───────────┘
                 │
                 ▼
        📤 Response 200 OK

2. PROCESSAMENTO (Background)
        
        ┌────────────────────┐
        │ Status: processing │
        └────────┬───────────┘
                 │
                 ├─────────────────────┐
                 │                     │
                 ▼                     ▼
        ┌────────────────────┐  ┌─────────────────┐
        │  AGENTE 1:         │  │  AGENTE 2:      │
        │  Transcrição       │  │  Sumarização    │
        │                    │  │                 │
        │ 1. Carregar modelo │  │ 1. Ler texto    │
        │ 2. Processar áudio │  │ 2. Carregar AI  │
        │ 3. Gerar texto     │  │ 3. Resumir      │
        │                    │  │ 4. Calcular     │
        │ ✅ ~3-5 segs       │  │    compressão   │
        │                    │  │                 │
        │ ✅ ~2-3 segs       │
        └────────┬───────────┘  └────────┬────────┘
                 │                       │
                 ▼                       ▼
        ┌────────────────────┐  ┌─────────────────┐
        │ Salvar em BD       │  │ Salvar em BD    │
        │ Transcriptions     │  │ Summarizations  │
        └────────┬───────────┘  └────────┬────────┘
                 │                       │
                 └───────────┬───────────┘
                             │
                             ▼
        ┌────────────────────────────────┐
        │ Status: completed              │
        └────────┬───────────────────────┘
                 │
                 ▼
        ┌────────────────────────────────┐
        │ Notificar Sistemas Externos:   │
        ├────────────────────────────────┤
        │ 1. Backend Principal           │
        │ 2. Frontend Web                │
        │ 3. App Móvel                   │
        └────────────────────────────────┘

3. CONSULTA DO RESULTADO

        GET /api/v1/audio/{audio_id}
                 │
                 ▼
        ┌────────────────────┐
        │ Buscar em BD:      │
        │ - AudioRecord      │
        │ - Transcription    │
        │ - Summarization    │
        └────────┬───────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ Montar Response    │
        │ com todos os dados │
        └────────┬───────────┘
                 │
                 ▼
        📥 Response 200 OK
```

## Modelos de IA Utilizados

### Transcrição
```
┌─────────────────────────────────────────┐
│  OpenAI Whisper (Recomendado)          │
├─────────────────────────────────────────┤
│ Model: whisper-1                        │
│ Input: Arquivo de áudio (mp3, wav...)   │
│ Output: Texto transcrito                │
│ Linguagem: Multilíngue (PT-BR suportado)│
│ Accuracy: ~95-98%                       │
│ Tempo: ~3-5 segundos por minuto de áudio│
│ Custo: $0.006 por minuto                │
└─────────────────────────────────────────┘

Alternativa (Modelo Local):
┌─────────────────────────────────────────┐
│  Whisper Base (OpenAI/Transformers)     │
├─────────────────────────────────────────┤
│ Model: openai/whisper-base              │
│ Tamanho: ~140 MB                        │
│ Memória: ~2 GB                          │
│ Tempo: ~1-2 minutos por minuto de áudio │
│ Custo: Grátis (local)                   │
└─────────────────────────────────────────┘
```

### Sumarização
```
┌─────────────────────────────────────────┐
│  BART Large CNN (Recomendado)           │
├─────────────────────────────────────────┤
│ Model: facebook/bart-large-cnn          │
│ Input: Texto (até 1024 tokens)          │
│ Output: Resumo (10-25% do original)     │
│ Linguagem: Inglês (ajustável)           │
│ Accuracy: ~85-90%                       │
│ Tempo: ~2-3 segundos                    │
│ Tamanho: ~1.6 GB                        │
│ Memória: ~4 GB                          │
└─────────────────────────────────────────┘

Alternativas:
┌─────────────────────────────────────────┐
│  T5 Base                                │
│  - Menor, mais rápido                   │
│  - Menos preciso                        │
│                                         │
│  DistilBART                             │
│  - 6x mais rápido                       │
│  - 90% da precisão                      │
└─────────────────────────────────────────┘
```

## Banco de Dados

### Schema PostgreSQL

```sql
-- Tabela de Registros de Áudio
CREATE TABLE audio_records (
    id UUID PRIMARY KEY,
    agent_id VARCHAR(100) NOT NULL,
    patient_id VARCHAR(100) NOT NULL,
    filename VARCHAR(255) NOT NULL,
    file_path VARCHAR(500),
    file_size INTEGER,
    file_format VARCHAR(10) NOT NULL,
    duration FLOAT,
    status ENUM('uploaded', 'processing', 'transcribed', 'summarized', 'completed', 'error'),
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    processed_at TIMESTAMP,
    notes VARCHAR(500),
    metadata JSONB
);

-- Tabela de Transcrições
CREATE TABLE transcriptions (
    id UUID PRIMARY KEY,
    audio_record_id UUID FOREIGN KEY,
    text TEXT NOT NULL,
    language VARCHAR(10),
    confidence FLOAT,
    model_used VARCHAR(100) NOT NULL,
    processing_time FLOAT,
    created_at TIMESTAMP DEFAULT NOW(),
    metadata JSONB
);

-- Tabela de Sumarizações
CREATE TABLE summarizations (
    id UUID PRIMARY KEY,
    audio_record_id UUID FOREIGN KEY,
    transcription_id UUID FOREIGN KEY,
    text TEXT NOT NULL,
    original_length INTEGER,
    summarized_length INTEGER,
    compression_ratio FLOAT,
    model_used VARCHAR(100) NOT NULL,
    processing_time FLOAT,
    created_at TIMESTAMP DEFAULT NOW(),
    metadata JSONB
);

-- Índices para performance
CREATE INDEX idx_audio_agent ON audio_records(agent_id);
CREATE INDEX idx_audio_patient ON audio_records(patient_id);
CREATE INDEX idx_audio_status ON audio_records(status);
CREATE INDEX idx_transcription_audio ON transcriptions(audio_record_id);
CREATE INDEX idx_summarization_audio ON summarizations(audio_record_id);
```

## Segurança

```
┌─────────────────────────────────────────────┐
│          Camadas de Segurança               │
├─────────────────────────────────────────────┤
│                                             │
│  1. VALIDAÇÃO (API Layer)                   │
│     - Tipos de arquivo                      │
│     - Tamanho máximo (50 MB)                │
│     - Campos obrigatórios                   │
│                                             │
│  2. AUTENTICAÇÃO (Opcional)                 │
│     - JWT Tokens                            │
│     - API Keys                              │
│                                             │
│  3. AUTORIZAÇÃO (RBAC)                      │
│     - Agent vs Patient data                 │
│     - Isolamento por tenant                 │
│                                             │
│  4. ARMAZENAMENTO (Banco)                   │
│     - Encrypted passwords                   │
│     - HTTPS conexões                        │
│     - Backups regulares                     │
│                                             │
│  5. NETWORK (Infra)                         │
│     - CORS configurado                      │
│     - Rate limiting                         │
│     - DDoS protection                       │
│                                             │
└─────────────────────────────────────────────┘
```

## Métricas de Performance

```
Requisição Típica:
┌─────────────────────────────┐
│ Upload de 1 minuto de áudio │
│                             │
│ Tempo: ~5-8 segundos        │
│ - Upload: ~1-2 seg          │
│ - Transcrição: ~2-4 seg     │
│ - Sumarização: ~1-2 seg     │
│                             │
│ Uso de Recursos:            │
│ - CPU: 60-80%               │
│ - Memória: ~3-4 GB          │
│ - Disco: ~50 MB/arquivo     │
│                             │
└─────────────────────────────┘

Escalabilidade:
┌─────────────────────────────┐
│ Com GPU (CUDA):             │
│ - 5-10x mais rápido         │
│ - Processamento paralelo    │
│ - Melhor throughput         │
│                             │
│ Sem GPU:                    │
│ - CPU-only: 2-3 min/áudio   │
│ - Sequencial                │
│ - Custo mais baixo          │
│                             │
└─────────────────────────────┘
```

## Integrações

```
┌─────────────────────────────────────────────┐
│     Backend Principal (FastAPI)             │
├─────────────────────────────────────────────┤
│ Recebe: POST /api/v1/audio/summarized       │
│ Payload:                                    │
│ {                                           │
│   "type": "audio_summarized",               │
│   "payload": {                              │
│     "audio_record_id": "...",               │
│     "agent_id": "...",                      │
│     "patient_id": "...",                    │
│     "transcription_text": "...",            │
│     "summarization_text": "...",            │
│     "processed_at": "..."                   │
│   }                                         │
│ }                                           │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│     Frontend (React/Vue)                    │
├─────────────────────────────────────────────┤
│ Recebe: POST /api/v1/audio/processed        │
│ Dados: Resultado do processamento           │
│ Ação: Atualizar UI em tempo real            │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│     App Móvel (React Native/Flutter)        │
├─────────────────────────────────────────────┤
│ Recebe: POST /api/v1/audio/ready            │
│ Notificação push com resumo                 │
│ Ação: Exibir notificação ao usuário         │
└─────────────────────────────────────────────┘
```

---

Diagrama atualizado em Janeiro 2024
